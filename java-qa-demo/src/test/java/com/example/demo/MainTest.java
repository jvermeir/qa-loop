package com.example.demo;

import org.junit.Test;
import static org.junit.Assert.*;

// Minimal test — exercises almost nothing, so JaCoCo reports near-zero coverage
public class MainTest {

    @Test
    public void testPlaceholder() {
        assertTrue(true);
    }
}
